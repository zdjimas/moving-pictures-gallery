Moving Pictures — Local Demo

This ZIP includes two ways to run the demo:

1) index.html + /assets (recommended)
   - Open the folder 'gallery-local-demo' and double-click 'index.html' (Mac).
   - On iPad, use a code preview app (Koder/Kodex) or AirDrop to a Mac, open 'index.html'.
   - Hover (desktop) or tap (iPad) the tile to preview; click/tap to open with sound.

2) inline-fallback.html (single file)
   - Open 'inline-fallback.html' in a browser.
   - Tap '▶ Load & Play' to start; use 🔇 ⏯ ↺ controls.
   - This version embeds the video in the page and works without separate assets.

Next: When satisfied, upload 'index.html' and the 'assets' folder to your GitHub repo root.
